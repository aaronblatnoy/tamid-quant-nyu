# Candidate NN — <slug>

## Proposed by
Phase <N> handoff, YYYY-MM-DD, during `feat/phase-<N>-<slug>` work.

## Problem
What is this candidate trying to solve? Tie it back to the v0 thesis
(backtested-and-validated TD3C tightness signal safe to trade with real
capital). If it doesn't move that forward, it probably belongs in the
backlog, not a promoted phase.

## Proposed scope (sketch)
A rough mission paragraph. Doesn't need to be a full phase contract yet —
full flesh-out happens if-and-when the candidate is promoted (phase 90 or
user-driven).

## Evidence this matters
- Concrete finding from a prior phase (handoff reference / commit)
- Or: hypothesis we'd need to test, with the minimum viable test
- Or: external ask (user requirement, regulatory requirement)

## Rough shape of acceptance criteria
Directional bullets — what would "done" look like? If it's vague, the
candidate isn't ready for promotion yet.

## Dependencies / blockers
- Data access we don't have yet?
- ADR decisions that aren't made?
- Other candidates that must land first?

## Estimated effort
`standard` | `max` (same dial as phase Metadata). Order-of-magnitude time
if known.

## Disposition
One of: `open` (needs review), `triaged-YYYY-MM-DD` (reviewed, left in
backlog), `promoted-to-phase-NN` (became a real phase), `archived` (rejected
with rationale; moved to archived_candidates.md).
