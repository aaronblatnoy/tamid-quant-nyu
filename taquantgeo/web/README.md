# web/ — Next.js dashboard

Placeholder. Populated in Phase 6.

Stack will be:
- Next.js 15 (App Router)
- Tailwind v4 + shadcn/ui
- DeckGL on Maplibre for the globe
- TradingView Lightweight Charts for time-series
- TanStack Query + Zustand for data fetching + client state

To initialize when Phase 6 starts:
```bash
cd web
pnpm create next-app@latest . --typescript --tailwind --app --src-dir
```
